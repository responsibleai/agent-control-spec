agent_control_specification_version: "0.4.0-alpha.1"
metadata:
  name: host-annotator-proof
annotators:
  content_safety:
    type: classifier
policies:
  gate:
    type: rego
    bundle: ./bundle
intervention_points:
  input:
    policy_target: "$snap.input"
    annotations:
      content_safety:
        from: "$target"
    policy:
      id: gate
      query: data.acs.decision
