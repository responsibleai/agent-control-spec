agent_control_specification_version: "0.4.0-alpha.1"
metadata:
  name: human-approval
policies:
  gate:
    type: rego
    bundle: ./policy.rego
    data_paths: [./config.json, ../common.rego, ../../lib/approval.rego]
    query: data.acs.packs.human_approval.verdict
intervention_points:
  pre_tool_call:
    policy_target: $.target
    policy: {id: gate}
