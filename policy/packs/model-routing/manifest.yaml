agent_control_specification_version: "0.4.0-alpha.1"
metadata:
  name: model-routing
policies:
  gate:
    type: rego
    bundle: ./policy.rego
    data_paths: [./config.json, ../common.rego]
    query: data.acs.packs.model_routing.verdict
intervention_points:
  pre_model_call:
    policy_target: $.target
    policy: {id: gate}
