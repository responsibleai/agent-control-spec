agent_control_specification_version: "0.4.0-alpha.1"
metadata:
  name: tool-integrity
policies:
  gate:
    type: rego
    bundle: ./policy.rego
    data_paths: [./config.json, ../common.rego]
    query: data.acs.packs.tool_integrity.verdict
intervention_points:
  pre_tool_call:
    policy_target: $.target
    policy: {id: gate}
