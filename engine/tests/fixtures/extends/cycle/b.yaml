agent_control_specification_version: "0.4.0-alpha.1"
extends:
  - a.yaml
policies:
  cycle_policy:
    type: test
intervention_points:
  input:
    policy_target: "$snap.input"
    policy:
      id: cycle_policy
