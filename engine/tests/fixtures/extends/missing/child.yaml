agent_control_specification_version: "0.4.0-alpha.1"
extends:
  - does-not-exist.yaml
policies:
  test_policy:
    type: test
intervention_points:
  input:
    policy_target: "$snap.input"
    policy:
      id: test_policy
